# Core scientific Python
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Tabular detectors
xgboost>=2.0
lightgbm>=4.0

# Data loading (TelecomTS, SpotLight)
datasets>=2.14
huggingface_hub>=0.20
pyarrow>=14.0

# Jupyter / notebook execution
jupyter>=1.0
nbformat>=5.7
nbconvert>=7.0
ipykernel>=6.0

# Deep / foundation-model audits (E13, E14)
# Optional: required only if you re-run E13 and E14.
# Comment out the block below if you do not have a GPU.
torch>=2.0
transformers>=4.30
accelerate>=0.20

# Utility
tqdm>=4.65
